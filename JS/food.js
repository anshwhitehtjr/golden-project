class Food {
    constructor(x, y) {
        this.sprite = createSprite(x, y, 20, 20);
        this.sprite.visible = false;
        this.collide = false;
    }

    display() {
        
    }
}